{
  "name": "site-sync-notifier",
  "version": "1.0.0",
  "private": true,
  "description": "Watches siteSyncWakes/{employeeId} in Firestore and sends the silent FCM wake push for Avant Elevators' Site Sync feature. Runs standalone on the VPS via PM2 — no Firebase Functions involved.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.6.0"
  }
}
