# Path to the Firebase service account key JSON on this VPS.
# Firebase Console → Project Settings (gear icon) → Service Accounts →
# "Generate new private key". Upload the downloaded file here (e.g. via scp)
# and point this at it. Keep it outside of any web-served directory.
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json
