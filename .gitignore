# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android / Capacitor (generated — not committed)

# iOS (not building yet)
ios/

# Android build outputs only
android/.gradle/
android/app/build/
android/build/
android/capacitor-cordova-android-plugins/build/
.claude/
functions/.env

# vps/site-sync-notifier — never commit the Firebase admin credential or local env
vps/site-sync-notifier/.env
vps/site-sync-notifier/service-account*.json

# vps/site-view-relay — never commit local secrets
vps/site-view-relay/.env
